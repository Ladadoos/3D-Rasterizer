using OpenTK;
using OpenTK.Graphics.OpenGL;

namespace Template
{
    class SceneGraph
    {
        public void Render(Camera camera)
        {
            // render all objects(meshes) in de heirarchy using cameraMAtrix
        }
    }
}
